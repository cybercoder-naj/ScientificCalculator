package com.github.cybercodernaj.calculator.parser

import com.github.cybercodernaj.calculator.Expression

internal object Parser {
    fun parse(function: String): Expression {
        val bracketRegex = Regex("")
    }
}